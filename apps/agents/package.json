{
  "name": "@mcp-relay-harness/agents",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env --experimental-strip-types src/index.ts",
    "demo:cache": "node --env-file=.env --experimental-strip-types src/cacheSharingAgent.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^5.6.0"
  }
}
